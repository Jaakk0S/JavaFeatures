# Java 10 New Features

The most important new features with Java 10 are:
- `var` type (type inference of local variables with initializers)
- Collectors API additions:
  - toUnmodifiableList
  - toUnmodifiableSet 
  - toUnmodifiableMap

